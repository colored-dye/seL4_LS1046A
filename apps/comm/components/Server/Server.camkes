import <Timer.idl4>;

component Server {
    include <smaccm_types.h>;
    control;
    
    has semaphore dispatch_sem;

    uses Timer timer;
    consumes Notification timer_complete;
    
    uses SMACCM_DATA__GIDL self2encrypt;
}
